export const TrezorTransactions = () => {
  const sendNativeToken = () => { };

  const sendCustomToken = () => { };

  const mintToken = () => { };

  const sendTransaction = () => { };

  return {
    sendNativeToken,
    sendCustomToken,
    mintToken,
    sendTransaction,
  }
};