export const TrezorTransactions = () => {
  const sendNativeToken = () => { };

  const sentCustomToken = () => { };

  const mintToken = () => { };

  const sendTransaction = () => { };

  return {
    sendNativeToken,
    sentCustomToken,
    mintToken,
    sendTransaction,
  }
};